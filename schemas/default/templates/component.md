# <Component Name>

## Role

<!--
Describe what this component is for.

Focus on the component's responsibility in the UI.
Explain what problem this component solves or what part of the page it is responsible for.
Do not describe the whole page here.
-->

## Displayed Information

<!--
List the information this component should show.

Describe only the information handled by this component.
Do not repeat page-level information unless this component is directly responsible for showing it.
Do not describe implementation details such as API fields or data types.
-->

- 

## Actions

<!--
List the actions available within this component.

Describe what the user can do through this component.
Include only actions that belong to this component.
Do not describe actions that belong to the page as a whole unless this component directly provides them.
-->

- 

## Layout

### Description

<!--
Short prose describing how the component is structured internally — the arrangement of its own content, information, and actions.

Examples:
- label on top, input below, helper text aligned right
- icon + label + count, in a single row
- header / body / footer with the primary action in the footer

Do not describe the full page layout here.
Do not go too deep into visual styling unless the structure depends on it.
-->

### Wireframe

<!--
Optional. Include an ASCII wireframe only when the component's internal structure is not self-evident from Description.
Skip this subsection (delete it entirely) for components whose structure is obvious — e.g., a single-element badge.

Conventions (same as pages, but scoped to the component's own elements):
- Use box-drawing characters: ┌ ┐ └ ┘ │ ─ ├ ┤ ┬ ┴
- Label elements in brackets: [Label], [Input], [Submit]
- Use … or repeated rows to indicate repetition
- Annotate dynamic regions briefly when helpful: [Input] (error), [Submit] (disabled)
- Stay at component scope — do NOT draw the surrounding page.
- The wireframe captures arrangement and density, NOT pixel-perfect styling.
-->

## States

<!--
List the states that must be considered for this component.

Include only states that affect what the user sees in this component.
Examples:
- default
- loading
- empty
- error
- disabled
- read-only
- expanded
- collapsed

If a state is not relevant, do not force it in.
-->

- 

## Notes

<!--
Optional component-specific constraints or reminders.

Use this only for information that should not be missed when designing or implementing this component.
Examples:
- reused across multiple pages
- should keep action order consistent
- should not expose destructive actions directly
-->
